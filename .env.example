# OpenAI
OPENAI_API_KEY=

# Langfuse (self-hosted — keys generated in the Langfuse UI on first boot)
LANGFUSE_SECRET_KEY=
LANGFUSE_PUBLIC_KEY=
LANGFUSE_BASE_URL=http://localhost:3000

# App database (Phase B uses it; defined now so the contract is complete)
DATABASE_URL=postgresql+asyncpg://app:app@localhost:5433/app

# Adzuna (job search, Phase D) — https://developer.adzuna.com
ADZUNA_APP_ID=
ADZUNA_API_KEY=
ADZUNA_COUNTRY=au

